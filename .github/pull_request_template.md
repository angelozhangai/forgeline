<!-- Issues, comments and PRs in this repository are written in English. See CONTRIBUTING.md. -->

## Why

What was wrong or missing. If the failure had no symptom — or a misleading one — say so; that is the
part reviewers cannot reconstruct from the diff.

## What changed

## Checklist

- [ ] `npm run ci` is green locally (`npm run hooks` has been run in this clone)
- [ ] Everything added is in English — code, comments, and this description
- [ ] Tests moved with the implementation; `TEST_COUNT_FLOOR` raised if tests were added
- [ ] No core file was patched to serve a downstream need (see the L0–L3 table in AGENTS.md)
- [ ] Anything left deliberately undone is stated above rather than left silent

Closes #
