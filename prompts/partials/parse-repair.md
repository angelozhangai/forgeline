Your previous output could not be parsed as the agreed JSON:

{{ERROR}}

Please re-emit **only** one valid ```json block — no text outside the block, no comments, no trailing commas, no nested code fences (inside long text like `tech_design_markdown`, use indentation or inline code instead of ``` fences). It must strictly match this contract:

{{CONTRACT}}
